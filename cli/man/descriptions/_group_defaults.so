The \fBcloudscheduler group defaults\fR action modifies and displays group defaults from csv2 servers.
This action is available to privileged users, provided they are in the group that the defaults belong to.
To view and modify defaults as an unpriviliged user use the \fBcloudscheduler metadata group-defaults\fR command.
