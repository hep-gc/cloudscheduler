view_redundant_machines:
    Synopsis:

    Keys:

    Columns:

        group_name:
            Format: String(32)
            Synopsis:

        hostname:
            Format: String(256)
            Synopsis:

        condor_host:
            Format: String(64)
            Synopsis:

        name:
            Format: String(128)
            Synopsis:

        entered_current_state:
            Format: Integer
            Synopsis:

        cloud_name:
            Format: String(32)
            Synopsis:

