view_metadata_collation:
    Synopsis:

    Keys:

    Columns:

        group_name:
            Format: String(32)
            Synopsis:

        cloud_name:
            Format: String(32)
            Synopsis:

        type:
            Format: String(5)
            Synopsis:

        priority:
            Format: Integer
            Synopsis:

        metadata_name:
            Format: String(64)
            Synopsis:

        mime_type:
            Format: String(128)
            Synopsis:

