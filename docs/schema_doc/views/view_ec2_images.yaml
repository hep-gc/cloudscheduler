view_ec2_images:
    Synopsis:

    Keys:

    Columns:

        region:
            Format: String(32)
            Synopsis:

        id:
            Format: String(128)
            Synopsis:

        borrower_id:
            Format: String(32)
            Synopsis:

        owner_id:
            Format: String(32)
            Synopsis:

        owner_alias:
            Format: String(64)
            Synopsis:

        disk_format:
            Format: String(128)
            Synopsis:

        size:
            Format: Integer
            Synopsis:

        image_location:
            Format: String(512)
            Synopsis:

        visibility:
            Format: String(128)
            Synopsis:

        name:
            Format: String(256)
            Synopsis:

        description:
            Format: String(256)
            Synopsis:

        last_updated:
            Format: Integer
            Synopsis:

        lower_location:
            Format: String(512)
            Synopsis:

        opsys:
            Format: String(8)
            Synopsis:

        arch:
            Format: String(5)
            Synopsis:

