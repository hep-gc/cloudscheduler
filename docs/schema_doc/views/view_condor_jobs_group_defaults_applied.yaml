view_condor_jobs_group_defaults_applied:
    Synopsis:
        timeseries poller and User Interface (UI)

    Keys:

    Columns:

        global_job_id:
            Format: String(128)
            Synopsis:
                Is the global job ID assigned by HTCondor.

        group_name:
            Format: String(32)
            Synopsis:
                Is the name of the group owning the job.

        target_clouds:
            Format: String
            Synopsis:
                Is the name of an alias identifying a subset of the group's clouds that
                this job should run on.

        job_status:
            Format: Integer
            Synopsis:

        request_cpus:
            Format: Integer
            Synopsis:

        request_disk:
            Format: Integer
            Synopsis:

        request_ram:
            Format: Integer
            Synopsis:

        request_swap:
            Format: Integer
            Synopsis:

        requirements:
            Format: String(512)
            Synopsis:

        job_priority:
            Format: Integer
            Synopsis:

        cluster_id:
            Format: Integer
            Synopsis:

        proc_id:
            Format: Integer
            Synopsis:

        user:
            Format: String(512)
            Synopsis:

        image:
            Format: String
            Synopsis:

        instance_type:
            Format: String(512)
            Synopsis:

        network:
            Format: String(512)
            Synopsis:

        keep_alive:
            Format: String(512)
            Synopsis:

        max_price:
            Format: String(512)
            Synopsis:

        user_data:
            Format: String(512)
            Synopsis:

        job_per_core:
            Format: Integer
            Synopsis:

        entered_current_status:
            Format: Integer
            Synopsis:

        q_date:
            Format: Integer
            Synopsis:

        hold_job_reason:
            Format: String(64)
            Synopsis:

        held_reason:
            Format: String(128)
            Synopsis:

        js_idle:
            Format: Integer
            Synopsis:

        js_running:
            Format: Integer
            Synopsis:

        js_completed:
            Format: Integer
            Synopsis:

        js_held:
            Format: Integer
            Synopsis:

        js_other:
            Format: Integer
            Synopsis:

