view_vm_kill_retire_priority_age:
    Synopsis:

    Keys:

    Columns:

        group_name:
            Format: String(32)
            Synopsis:

        cloud_name:
            Format: String(32)
            Synopsis:

        vmid:
            Format: String(128)
            Synopsis:

        flavor_id:
            Format: String(128)
            Synopsis:

        machine:
            Format: String(256)
            Synopsis:

        killed:
            Format: Integer
            Synopsis:

        retired:
            Format: Integer
            Synopsis:

        priority:
            Format: Integer
            Synopsis:

        flavor_cores:
            Format: Integer
            Synopsis:

        flavor_ram:
            Format: Integer
            Synopsis:

