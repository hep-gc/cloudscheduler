view_groups_with_metadata_info:
    Synopsis:

    Keys:

    Columns:

        group_name:
            Format: String(32)
            Synopsis:

        htcondor_fqdn:
            Format: String(128)
            Synopsis:

        htcondor_container_hostname:
            Format: String(128)
            Synopsis:

        htcondor_other_submitters:
            Format: String(128)
            Synopsis:

        metadata_name:
            Format: String(64)
            Synopsis:

        metadata_enabled:
            Format: Boolean
            Synopsis:

        metadata_priority:
            Format: Integer
            Synopsis:

        metadata_mime_type:
            Format: String(128)
            Synopsis:

