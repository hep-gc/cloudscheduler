view_foreign_flavors:
    Synopsis:

    Keys:

    Columns:

        group_name:
            Format: String(32)
            Synopsis:

        cloud_name:
            Format: String(32)
            Synopsis:

        authurl:
            Format: String(128)
            Synopsis:

        region:
            Format: String(32)
            Synopsis:

        project:
            Format: String(128)
            Synopsis:

        flavor_id:
            Format: String(128)
            Synopsis:

        count:
            Format: Integer
            Synopsis:

        name:
            Format: String(128)
            Synopsis:

        cores:
            Format: Integer
            Synopsis:

        ram:
            Format: Float
            Synopsis:

