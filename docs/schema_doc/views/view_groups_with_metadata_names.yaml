view_groups_with_metadata_names:
    Synopsis:

    Keys:

    Columns:

        group_name:
            Format: String(32)
            Synopsis:

        htcondor_fqdn:
            Format: String(128)
            Synopsis:

        htcondor_container_hostname:
            Format: String(128)
            Synopsis:

        htcondor_other_submitters:
            Format: String(128)
            Synopsis:

        metadata_names:
            Format: String
            Synopsis:

