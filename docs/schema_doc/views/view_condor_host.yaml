view_condor_host:
    Synopsis:

    Keys:

    Columns:

        group_name:
            Format: String(32)
            Synopsis:

        cloud_name:
            Format: String(32)
            Synopsis:

        htcondor_fqdn:
            Format: String(128)
            Synopsis:

        vmid:
            Format: String(128)
            Synopsis:

        hostname:
            Format: String(128)
            Synopsis:

        primary_slots:
            Format: Integer
            Synopsis:

        dynamic_slots:
            Format: Integer
            Synopsis:

        retire:
            Format: Integer
            Synopsis:

        retiring:
            Format: Integer
            Synopsis:

        terminate:
            Format: Integer
            Synopsis:

        machine:
            Format: String(256)
            Synopsis:

        updater:
            Format: String(128)
            Synopsis:

