view_cloud_status_slot_summary:
    Synopsis:
        "
        REF=(common/view_cloud_status)
        
        REF=(common/view_cloud_status_slots)

        REF=(common/view_cloud_status_slots/Columns/slot_summary)
        "

    Keys:

    Columns:

        group_name:
            Format: String(32)
            Synopsis:
                REF=(views/view_cloud_status_flavor_slot_detail_summary/Columns/group_name)

        cloud_name:
            Format: String(32)
            Synopsis:
                REF=(views/view_cloud_status_flavor_slot_detail_summary/Columns/cloud_name)

        busy:
            Format: Integer
            Synopsis:
                REF=(views/view_cloud_status_flavor_slot_summary/Columns/busy)

        idle:
            Format: Integer
            Synopsis:
                REF=(views/view_cloud_status_flavor_slot_summary/Columns/idle)

        idle_percent:
            Format: Integer
            Synopsis:
                REF=(views/view_cloud_status_flavor_slot_summary/Columns/idle_percent)

