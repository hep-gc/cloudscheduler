view_cloud_status_slot_summary:
    Synopsis:

    Keys:

    Columns:

        group_name:
            Format: String(32)
            Synopsis:

        cloud_name:
            Format: String(32)
            Synopsis:

        busy:
            Format: Integer
            Synopsis:

        idle:
            Format: Integer
            Synopsis:

        idle_percent:
            Format: Integer
            Synopsis:

