view_cloud_status_flavor_slot_detail_summary:
    Synopsis:

    Keys:

    Columns:

        group_name:
            Format: String(32)
            Synopsis:

        cloud_name:
            Format: String(32)
            Synopsis:

        flavor:
            Format: String(46)
            Synopsis:

        slot_type:
            Format: Integer
            Synopsis:

        slot_count:
            Format: Integer
            Synopsis:

        core_count:
            Format: Integer
            Synopsis:

