view_ec2_instance_types:
    Synopsis:

    Keys:

    Columns:

        region:
            Format: String(32)
            Synopsis:

        instance_type:
            Format: String(32)
            Synopsis:

        operating_system:
            Format: String(32)
            Synopsis:

        instance_family:
            Format: String(32)
            Synopsis:

        processor:
            Format: String(64)
            Synopsis:

        storage:
            Format: String(32)
            Synopsis:

        cores:
            Format: Integer
            Synopsis:

        memory:
            Format: Float
            Synopsis:

        cost_per_hour:
            Format: Float
            Synopsis:

        memory_per_core:
            Format: Float
            Synopsis:

        processor_manufacturer:
            Format: String(64)
            Synopsis:

