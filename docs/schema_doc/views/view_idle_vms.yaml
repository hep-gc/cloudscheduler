view_idle_vms:
    Synopsis:
        "
        REF=(common/view_vm_scheduling)

        The **view_idle_vms** is used
        "


    Keys:

    Columns:

        group_name:
            Format: String(32)
            Synopsis:

        cloud_name:
            Format: String(32)
            Synopsis:

        come_alive:
            Format: String(128)
            Synopsis:

        job_alive:
            Format: String(128)
            Synopsis:

        error_delay:
            Format: String(128)
            Synopsis:

        keep_alive:
            Format: Integer
            Synopsis:

        vmid:
            Format: String(128)
            Synopsis:

        hostname:
            Format: String(128)
            Synopsis:

        primary_slots:
            Format: Integer
            Synopsis:

        dynamic_slots:
            Format: Integer
            Synopsis:

        retire:
            Format: Integer
            Synopsis:

        terminate:
            Format: Integer
            Synopsis:

        poller_status:
            Format: String(12)
            Synopsis:

        age:
            Format: Integer
            Synopsis:

