csv2_user:
    Synopsis:

    Keys:

        username:
            Format: String(32)
            Synopsis:

    Columns:

        cert_cn:
            Format: String(128)
            Synopsis:

        password:
            Format: String(128)
            Synopsis:

        is_superuser:
            Format: Boolean
            Synopsis:

        join_date:
            Format: Integer
            Synopsis:

        default_group:
            Format: String(32)
            Synopsis:

        flag_global_status:
            Format: Boolean
            Synopsis:

        status_refresh_interval:
            Format: Integer
            Synopsis:

        flag_show_slot_detail:
            Format: Boolean
            Synopsis:

        flag_show_slot_flavors:
            Format: Boolean
            Synopsis:

