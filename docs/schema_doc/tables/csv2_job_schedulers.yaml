csv2_job_schedulers:
    Synopsis:

    Keys:

        htcondor_fqdn:
            Format: String(128)
            Synopsis:

    Columns:

        condor_status:
            Format: Boolean
            Synopsis:

        agent_status:
            Format: Boolean
            Synopsis:

        foreign_jobs:
            Format: Integer
            Synopsis:

