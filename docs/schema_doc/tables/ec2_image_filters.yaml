ec2_image_filters:
    Synopsis:

    Keys:

        group_name:
            Format: String(32)
            Synopsis:

        cloud_name:
            Format: String(32)
            Synopsis:

    Columns:

        owner_aliases:
            Format: String(128)
            Synopsis:

        owner_ids:
            Format: String(128)
            Synopsis:

        like:
            Format: String(128)
            Synopsis:

        not_like:
            Format: String(128)
            Synopsis:

        operating_systems:
            Format: String(128)
            Synopsis:

        architectures:
            Format: String(128)
            Synopsis:

