csv2_image_cache:
    Synopsis:

    Keys:

        image_name:
            Format: String(256)
            Synopsis:

        checksum:
            Format: String(64)
            Synopsis:

    Columns:

        container_format:
            Format: String(128)
            Synopsis:

        disk_format:
            Format: String(128)
            Synopsis:

        downloaded:
            Format: Integer
            Synopsis:

