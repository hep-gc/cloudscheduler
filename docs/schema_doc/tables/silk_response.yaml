silk_response:
    Synopsis:

    Keys:

        id:
            Format: String(36)
            Synopsis:

    Columns:

        status_code:
            Format: Integer
            Synopsis:

        raw_body:
            Format: String
            Synopsis:

        body:
            Format: String
            Synopsis:

        encoded_headers:
            Format: String
            Synopsis:

        request_id:
            Format: String(36)
            Synopsis:

