csv2_group_metadata:
    Synopsis:

    Keys:

        group_name:
            Format: String(32)
            Synopsis:

        metadata_name:
            Format: String(64)
            Synopsis:

    Columns:

        enabled:
            Format: Boolean
            Synopsis:

        priority:
            Format: Integer
            Synopsis:

        metadata:
            Format: String
            Synopsis:

        mime_type:
            Format: String(128)
            Synopsis:

