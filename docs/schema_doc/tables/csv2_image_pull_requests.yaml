csv2_image_pull_requests:
    Synopsis:

    Keys:

        tx_id:
            Format: Integer
            Synopsis:

    Columns:

        target_group_name:
            Format: String(128)
            Synopsis:

        target_cloud_name:
            Format: String(128)
            Synopsis:

        image_name:
            Format: String(128)
            Synopsis:

        image_id:
            Format: String(128)
            Synopsis:

        checksum:
            Format: Integer
            Synopsis:

        status:
            Format: String(32)
            Synopsis:

        message:
            Format: String(512)
            Synopsis:

        request_time:
            Format: Integer
            Synopsis:

        requester:
            Format: String(64)
            Synopsis:

