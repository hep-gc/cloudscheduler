cloud_security_groups:
    Synopsis:

    Keys:

        group_name:
            Format: String(32)
            Synopsis:

        cloud_name:
            Format: String(32)
            Synopsis:

        id:
            Format: String(64)
            Synopsis:

    Columns:

        name:
            Format: String(64)
            Synopsis:

        cloud_type:
            Format: String(64)
            Synopsis:

        last_updated:
            Format: Integer
            Synopsis:

