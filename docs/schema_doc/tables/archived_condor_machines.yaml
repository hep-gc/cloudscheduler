archived_condor_machines:
    Synopsis:

    Keys:

        name:
            Format: String(128)
            Synopsis:

    Columns:

        machine:
            Format: String(256)
            Synopsis:

        group_name:
            Format: String(32)
            Synopsis:

        condor_host:
            Format: String(64)
            Synopsis:

        flavor:
            Format: String(32)
            Synopsis:

        job_id:
            Format: String(128)
            Synopsis:

        global_job_id:
            Format: String(128)
            Synopsis:

        address:
            Format: String(512)
            Synopsis:

        state:
            Format: String(128)
            Synopsis:

        activity:
            Format: String(128)
            Synopsis:

        vm_type:
            Format: String(128)
            Synopsis:

        my_current_time:
            Format: Integer
            Synopsis:

        entered_current_state:
            Format: Integer
            Synopsis:

        start:
            Format: String(128)
            Synopsis:

        remote_owner:
            Format: String(128)
            Synopsis:

        slot_type:
            Format: String(128)
            Synopsis:

        total_slots:
            Format: Integer
            Synopsis:

        idle_time:
            Format: Integer
            Synopsis:

        retire_request_time:
            Format: Integer
            Synopsis:

        retired_time:
            Format: Integer
            Synopsis:

