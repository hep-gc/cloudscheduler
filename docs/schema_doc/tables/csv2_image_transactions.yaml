csv2_image_transactions:
    Synopsis:

    Keys:

        tx_id:
            Format: String(16)
            Synopsis:

    Columns:

        status:
            Format: String(128)
            Synopsis:

        message:
            Format: String(128)
            Synopsis:

        target_group_name:
            Format: String(128)
            Synopsis:

        target_cloud_name:
            Format: String(128)
            Synopsis:

        image_name:
            Format: String(128)
            Synopsis:

        image_id:
            Format: String(128)
            Synopsis:

        checksum:
            Format: String(64)
            Synopsis:

        request_time:
            Format: Integer
            Synopsis:

        requester:
            Format: String(64)
            Synopsis:

