condor_jobs:
    Synopsis:

    Keys:

        global_job_id:
            Format: String(128)
            Synopsis:

    Columns:

        group_name:
            Format: String(32)
            Synopsis:

        target_clouds:
            Format: String
            Synopsis:

        target_alias:
            Format: String(32)
            Synopsis:

        job_status:
            Format: Integer
            Synopsis:

        request_cpus:
            Format: Integer
            Synopsis:

        request_ram:
            Format: Integer
            Synopsis:

        request_disk:
            Format: Integer
            Synopsis:

        request_swap:
            Format: Integer
            Synopsis:

        request_scratch:
            Format: Integer
            Synopsis:

        requirements:
            Format: String(512)
            Synopsis:

        job_priority:
            Format: Integer
            Synopsis:

        cluster_id:
            Format: Integer
            Synopsis:

        proc_id:
            Format: Integer
            Synopsis:

        user:
            Format: String(512)
            Synopsis:

        image:
            Format: String
            Synopsis:

        instance_type:
            Format: String(512)
            Synopsis:

        network:
            Format: String(512)
            Synopsis:

        keep_alive:
            Format: String(512)
            Synopsis:

        max_price:
            Format: String(512)
            Synopsis:

        user_data:
            Format: String(512)
            Synopsis:

        job_per_core:
            Format: Integer
            Synopsis:

        entered_current_status:
            Format: Integer
            Synopsis:

        q_date:
            Format: Integer
            Synopsis:

        hold_reason_code:
            Format: Boolean
            Synopsis:

        hold_reason_subcode:
            Format: Boolean
            Synopsis:

        last_remote_host:
            Format: String(64)
            Synopsis:

        held_reason:
            Format: String(128)
            Synopsis:

        hold_job_reason:
            Format: String(64)
            Synopsis:

