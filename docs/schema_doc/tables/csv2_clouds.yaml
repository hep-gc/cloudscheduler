csv2_clouds:
    Synopsis:

    Keys:

        group_name:
            Format: String(32)
            Synopsis:

        cloud_name:
            Format: String(32)
            Synopsis:

    Columns:

        enabled:
            Format: Boolean
            Synopsis:

        priority:
            Format: Integer
            Synopsis:

        authurl:
            Format: String(128)
            Synopsis:

        project:
            Format: String(128)
            Synopsis:

        username:
            Format: String(20)
            Synopsis:

        password:
            Format: String
            Synopsis:

        keyname:
            Format: String(20)
            Synopsis:

        cacertificate:
            Format: String
            Synopsis:

        region:
            Format: String(20)
            Synopsis:

        user_domain_name:
            Format: String(20)
            Synopsis:

        user_domain_id:
            Format: String(64)
            Synopsis:

        project_domain_name:
            Format: String(20)
            Synopsis:

        project_domain_id:
            Format: String(64)
            Synopsis:

        cloud_type:
            Format: String(64)
            Synopsis:

        ec2_owner_id:
            Format: String(32)
            Synopsis:

        server_meta_ctl:
            Format: Integer
            Synopsis:

        instances_ctl:
            Format: Integer
            Synopsis:

        personality_ctl:
            Format: Integer
            Synopsis:

        image_meta_ctl:
            Format: Integer
            Synopsis:

        personality_size_ctl:
            Format: Integer
            Synopsis:

        ram_ctl:
            Format: Integer
            Synopsis:

        server_groups_ctl:
            Format: Integer
            Synopsis:

        security_group_rules_ctl:
            Format: Integer
            Synopsis:

        keypairs_ctl:
            Format: Integer
            Synopsis:

        security_groups_ctl:
            Format: Integer
            Synopsis:

        server_group_members_ctl:
            Format: Integer
            Synopsis:

        floating_ips_ctl:
            Format: Integer
            Synopsis:

        cores_ctl:
            Format: Integer
            Synopsis:

        cores_softmax:
            Format: Integer
            Synopsis:

        spot_price:
            Format: Float
            Synopsis:

        vm_flavor:
            Format: String(64)
            Synopsis:

        vm_image:
            Format: String(64)
            Synopsis:

        vm_keep_alive:
            Format: Integer
            Synopsis:

        vm_keyname:
            Format: String(64)
            Synopsis:

        vm_network:
            Format: String(64)
            Synopsis:

        vm_security_groups:
            Format: String(128)
            Synopsis:

        error_count:
            Format: Integer
            Synopsis:

        error_time:
            Format: Integer
            Synopsis:

        network_up:
            Format: Boolean
            Synopsis:

        network_rtt:
            Format: Integer
            Synopsis:

