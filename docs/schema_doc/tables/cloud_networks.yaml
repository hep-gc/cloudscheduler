cloud_networks:
    Synopsis:

    Keys:

        group_name:
            Format: String(32)
            Synopsis:

        cloud_name:
            Format: String(32)
            Synopsis:

        id:
            Format: String(128)
            Synopsis:

    Columns:

        name:
            Format: String(256)
            Synopsis:

        cloud_type:
            Format: String(64)
            Synopsis:

        subnets:
            Format: String(256)
            Synopsis:

        tenant_id:
            Format: String(128)
            Synopsis:

        external_route:
            Format: Boolean
            Synopsis:

        shared:
            Format: Boolean
            Synopsis:

        last_updated:
            Format: Integer
            Synopsis:

