auth_user:
    Synopsis:

    Keys:

        id:
            Format: Integer
            Synopsis:

    Columns:

        password:
            Format: String(128)
            Synopsis:

        last_login:
            Format: Integer
            Synopsis:

        is_superuser:
            Format: Boolean
            Synopsis:

        username:
            Format: String(150)
            Synopsis:

        first_name:
            Format: String(30)
            Synopsis:

        last_name:
            Format: String(150)
            Synopsis:

        email:
            Format: String(254)
            Synopsis:

        is_staff:
            Format: Boolean
            Synopsis:

        is_active:
            Format: Boolean
            Synopsis:

        date_joined:
            Format: Integer
            Synopsis:

