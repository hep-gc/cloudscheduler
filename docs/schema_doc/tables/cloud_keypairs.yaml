cloud_keypairs:
    Synopsis:

    Keys:

        group_name:
            Format: String(32)
            Synopsis:

        cloud_name:
            Format: String(32)
            Synopsis:

        fingerprint:
            Format: String(64)
            Synopsis:

        key_name:
            Format: String(32)
            Synopsis:

    Columns:

        cloud_type:
            Format: String(64)
            Synopsis:

