cloud_images:
    Synopsis:

    Keys:

        group_name:
            Format: String(32)
            Synopsis:

        cloud_name:
            Format: String(32)
            Synopsis:

        id:
            Format: String(128)
            Synopsis:

    Columns:

        cloud_type:
            Format: String(64)
            Synopsis:

        container_format:
            Format: String(128)
            Synopsis:

        disk_format:
            Format: String(128)
            Synopsis:

        name:
            Format: String(256)
            Synopsis:

        size:
            Format: Integer
            Synopsis:

        visibility:
            Format: String(128)
            Synopsis:

        min_disk:
            Format: Integer
            Synopsis:

        min_ram:
            Format: Integer
            Synopsis:

        checksum:
            Format: String(64)
            Synopsis:

        last_updated:
            Format: Integer
            Synopsis:

