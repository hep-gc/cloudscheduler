cloud_flavors:
    Synopsis:

    Keys:

        group_name:
            Format: String(32)
            Synopsis:

        cloud_name:
            Format: String(32)
            Synopsis:

        id:
            Format: String(128)
            Synopsis:

    Columns:

        name:
            Format: String(128)
            Synopsis:

        cloud_type:
            Format: String(64)
            Synopsis:

        ram:
            Format: Integer
            Synopsis:

        cores:
            Format: Integer
            Synopsis:

        swap:
            Format: Integer
            Synopsis:

        disk:
            Format: Integer
            Synopsis:

        ephemeral_disk:
            Format: Integer
            Synopsis:

        is_public:
            Format: Boolean
            Synopsis:

        last_updated:
            Format: Integer
            Synopsis:

