csv2_service_catalog:
    Synopsis:

    Keys:

        service:
            Format: String(64)
            Synopsis:

        fqdn:
            Format: String(128)
            Synopsis:

    Columns:

        last_updated:
            Format: Integer
            Synopsis:

        flag_htcondor_allow:
            Format: Boolean
            Synopsis:

        yaml_attribute_name:
            Format: String(64)
            Synopsis:

