/*
** Consolidate group/cloud summary information into one row per group/cloud.
*/
create or replace view view_group_list as

select

group_name,
cloud_name,
sum(VMs) as VMs,
sum(Jobs) as Jobs

from view_group_list_raw
group by group_name,cloud_name

;
